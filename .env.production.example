## Production environment template for Formula1Predictions
## Copy these values into your hosting provider's environment settings (e.g. Railway Variables)
## and replace placeholder values with real secrets. Do NOT commit real credentials.

APP_NAME="F1 Predictions"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://your-production-domain.example

# First admin setup (for deploy):
# Set these in production env (Railway secrets, etc.), then run:
#   php artisan db:seed --class=AdminSeeder
ADMIN_EMAIL=your-admin@example.com
ADMIN_PASSWORD=your-secure-password-here
ADMIN_NAME=Your Admin Name

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

# Logging: use stack channel with daily rotation and minimal verbosity in production
LOG_CHANNEL=stack
LOG_STACK=daily
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning

# Database — typical MySQL production configuration.
# For Railway, you can either:
# - Use DATABASE_URL, or
# - Reference service variables (see docs) and set the values here.
DB_CONNECTION=mysql
DB_HOST=your-db-host.example
DB_PORT=3306
DB_DATABASE=formula1predictions
DB_USERNAME=formula1predictions
DB_PASSWORD=your-db-password-here

# Session: secure, encrypted cookies for production.
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_SECURE_COOKIE=true
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
CACHE_STORE=database

# Mail: configure a real mail transport (smtp, mailgun, ses, etc.) for production.
MAIL_MAILER=smtp
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=your-smtp-username
MAIL_PASSWORD=your-smtp-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"

# F1 Configuration
F1_MAX_DRIVERS=22
F1_MAX_TEAMS=11
F1_DEFAULT_APPEARANCE=system

